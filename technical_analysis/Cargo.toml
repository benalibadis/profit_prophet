[package]
name = "technical_analysis"
version = "0.0.0"
edition = "2021"

[dependencies]
rust_decimal = { version = "1.35", optional = true }

[dev-dependencies]

[features]
default = []
simd = []
precision = ["rust_decimal"]
unsafe = []